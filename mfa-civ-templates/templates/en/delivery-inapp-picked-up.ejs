Your passport<% if (typeof passportId !== 'undefined' && passportId) { %> (<%= passportId %>)<% } %> has been picked up by the courier and is on its way to the sorting facility.<% if (typeof trackingNumber !== 'undefined' && trackingNumber) { %> Tracking: <%= trackingNumber %><% } %>