Your passport<% if (typeof passportId !== 'undefined' && passportId) { %> (<%= passportId %>)<% } %> is currently in transit and on its way to your delivery address.<% if (typeof trackingNumber !== 'undefined' && trackingNumber) { %> Tracking: <%= trackingNumber %><% } %>